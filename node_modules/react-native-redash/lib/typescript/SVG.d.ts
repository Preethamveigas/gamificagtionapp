import Animated from "react-native-reanimated";
interface Parts {
    search: {
        startX: number;
        endX: number;
        startY: number;
        endY: number;
    }[];
    startX: number[];
    startY: number[];
    endX: number[];
    endY: number[];
}
export declare const getParts: (d: string) => Parts;
export declare const getY: (d: string | Parts, x: Animated.Node<number>) => Animated.Node<number>;
export declare const getX: (d: string | Parts, y: Animated.Node<number>) => Animated.Node<number>;
export {};
